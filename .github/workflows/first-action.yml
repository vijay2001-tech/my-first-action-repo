name: First workflow
on: workflow_dispatch
jobs:
 first-job:
   runs-on: ubuntu-latest
   steps:
     - name: Print greetings and goodbye
       run: |
        echo "Hello world"
        echo " Good Bye"
        
